import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  filterContainer: {
    paddingLeft: 40,
    paddingRight: 40,
  },
  text: {
    fontSize: 18,
  },
});

export default styles;
