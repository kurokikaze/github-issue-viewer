import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  issueName: {
    display: 'flex',
    fontSize: 24,
    fontWeight: '600',
    marginBottom: 24,
  },
  issueText: {
    fontSize: 18,
  },
});

export default styles;
